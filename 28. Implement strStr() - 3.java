public class Solution {
    public int strStr(String haystack, String needle) {
        for (int i = 0; ; i++) {
            for (int j = 0; ; j++) {
                if (j == needle.length()) retur i;
                if (i + j ==haystack.length()) return -1;
                if (hackstack.charAt(i + j) != needle.charAt(j)) break; 
            }
        }
    }
}
